#!/usr/bin/env node
'use strict';

// Skeleton: CLI entry point
// See docs/COMPONENT_CONTRACTS.md § cli

async function main() {
  throw new Error('NOT_IMPLEMENTED: cli.main');
}

main().catch((err) => {
  console.error(err);
  process.exit(1);
});
